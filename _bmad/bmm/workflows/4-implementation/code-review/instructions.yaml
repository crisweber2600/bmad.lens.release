<workflow>
  <critical>The workflow execution engine is governed by: bmad.lens.release/_bmad/core/tasks/workflow.yaml</critical>
  <critical>You MUST have already loaded and processed: {installed_path}/workflow.yaml</critical>
  <critical>Communicate all responses in {communication_language} and language MUST be tailored to {user_skill_level}</critical>
  <critical>Generate all documents in {document_output_language}</critical>

  <critical>🔥 YOU ARE AN ADVERSARIAL CODE REVIEWER - Find what's wrong or missing! 🔥</critical>
  <critical>Your purpose: Validate story file claims against actual implementation</critical>
  <critical>Challenge everything: Are tasks marked [x] actually done? Are ACs really implemented?</critical>
  <critical>Find 3-10 specific issues in every review minimum - no lazy "looks good" reviews - YOU are so much better than the dev agent
    that wrote this slop</critical>
  <critical>Read EVERY file in the File List - verify implementation against story requirements</critical>
  <critical>Tasks marked complete but not done = CRITICAL finding</critical>
  <critical>Acceptance Criteria not implemented = HIGH severity finding</critical>
  <critical>Do not review files that are not part of the application's source code. Always exclude the _bmad/ and _bmad-output/ folders from the review. Always exclude IDE and CLI configuration folders like .cursor/ and .windsurf/ and .claude/</critical>


  <step n="1" goal="Load story and discover changes">
    <action>Use provided {{story_path}} or ask user which story file to review</action>
    <action>Read COMPLETE story file</action>
    <action>Set {{story_key}} = extracted key from filename (e.g., "1-2-user-authentication.md" → "1-2-user-authentication") or story
      metadata</action>
    <action>Parse sections: Story, Acceptance Criteria, Tasks/Subtasks, Dev Agent Record → File List, Change Log</action>

    <!-- Discover actual changes via git -->
    <action>Check if git repository detected in current directory</action>
    <check if="git repository exists">
      <action>Run `git status --porcelain` to find uncommitted changes</action>
      <action>Run `git diff --name-only` to see modified files</action>
      <action>Run `git diff --cached --name-only` to see staged files</action>
      <action>Compile list of actually changed files from git output</action>
    </check>

    <!-- Cross-reference story File List vs git reality -->
    <action>Compare story's Dev Agent Record → File List with actual git changes</action>
    <action>Note discrepancies:
      - Files in git but not in story File List
      - Files in story File List but no git changes
      - Missing documentation of what was actually changed
    </action>

    <invoke-protocol name="discover_inputs" />
    <action>Load {project_context} for coding standards (if exists)</action>
  </step>

  <step n="2" goal="Build review attack plan">
    <action>Extract ALL Acceptance Criteria from story</action>
    <action>Extract ALL Tasks/Subtasks with completion status ([x] vs [ ])</action>
    <action>From Dev Agent Record → File List, compile list of claimed changes</action>

    <action>Create review plan:
      1. **AC Validation**: Verify each AC is actually implemented
      2. **Task Audit**: Verify each [x] task is really done
      3. **Code Quality**: Security, performance, maintainability
      4. **Test Quality**: Real tests vs placeholder bullshit
    </action>
  </step>

  <step n="3" goal="Execute adversarial review">
    <critical>VALIDATE EVERY CLAIM - Check git reality vs story claims</critical>

    <!-- Git vs Story Discrepancies -->
    <action>Review git vs story File List discrepancies:
      1. **Files changed but not in story File List** → MEDIUM finding (incomplete documentation)
      2. **Story lists files but no git changes** → HIGH finding (false claims)
      3. **Uncommitted changes not documented** → MEDIUM finding (transparency issue)
    </action>

    <!-- Use combined file list: story File List + git discovered files -->
    <action>Create comprehensive review file list from story File List and git changes</action>

    <!-- AC Validation -->
    <action>For EACH Acceptance Criterion:
      1. Read the AC requirement
      2. Search implementation files for evidence
      3. Determine: IMPLEMENTED, PARTIAL, or MISSING
      4. If MISSING/PARTIAL → HIGH SEVERITY finding
    </action>

    <!-- Task Completion Audit -->
    <action>For EACH task marked [x]:
      1. Read the task description
      2. Search files for evidence it was actually done
      3. **CRITICAL**: If marked [x] but NOT DONE → CRITICAL finding
      4. Record specific proof (file:line)
    </action>

    <!-- Code Quality Deep Dive -->
    <action>For EACH file in comprehensive review list:
      1. **Security**: Look for injection risks, missing validation, auth issues
      2. **Performance**: N+1 queries, inefficient loops, missing caching
      3. **Error Handling**: Missing try/catch, poor error messages
      4. **Code Quality**: Complex functions, magic numbers, poor naming
      5. **Test Quality**: Are tests real assertions or placeholders?
    </action>

    <check if="total_issues_found lt 3">
      <critical>NOT LOOKING HARD ENOUGH - Find more problems!</critical>
      <action>Re-examine code for:
        - Edge cases and null handling
        - Architecture violations
        - Documentation gaps
        - Integration issues
        - Dependency problems
        - Git commit message quality (if applicable)
      </action>
      <action>Find at least 3 more specific, actionable issues</action>
    </check>
  </step>

  <step n="4" goal="Present findings and resolve them (auto default, manual retained)">
    <action>Categorize findings: HIGH (must fix), MEDIUM (should fix), LOW (nice to fix)</action>
    <action>Set {{fixed_count}} = 0</action>
    <action>Set {{action_count}} = 0</action>
    <action>Set {{review_outcome}} = "needs_manual"</action>

    <check if="review_pass is not set">
      <action>Set {{review_pass}} = 1</action>
    </check>

    <action>Set {{resolved_auto_fix_severities}} = {{auto_fix_severities}}</action>
    <check if="{{resolved_auto_fix_severities}} is empty">
      <action>Set {{resolved_auto_fix_severities}} = "CRITICAL,HIGH,MEDIUM"</action>
    </check>

    <output>**🔥 CODE REVIEW FINDINGS, {user_name}!**

      **Story:** {{story_file}}
      **Pass:** {{review_pass}} / {{max_review_passes}}
      **Git vs Story Discrepancies:** {{git_discrepancy_count}} found
      **Issues Found:** {{high_count}} High, {{medium_count}} Medium, {{low_count}} Low
      **Auto-fix Severities:** {{resolved_auto_fix_severities}}

      ## 🔴 CRITICAL ISSUES
      - Tasks marked [x] but not actually implemented
      - Acceptance Criteria not implemented
      - Story claims files changed but no git evidence
      - Security vulnerabilities

      ## 🟡 MEDIUM ISSUES
      - Files changed but not documented in story File List
      - Uncommitted changes not tracked
      - Performance problems
      - Poor test coverage/quality
      - Code maintainability issues

      ## 🟢 LOW ISSUES
      - Code style improvements
      - Documentation gaps
      - Git commit message quality
    </output>

    <check if="{{auto_fix_rerun}} == true">
      <action>Fix issues in severity set {{resolved_auto_fix_severities}}</action>
      <action>Add/update tests as needed</action>
      <action>Update File List in story if files changed</action>
      <action>Update story Dev Agent Record with fixes applied</action>
      <action>Set {{fixed_count}} = number of issues fixed in {{resolved_auto_fix_severities}}</action>
      <action>Set {{action_count}} = 0</action>

      <check if="fix-required issues remain in {{resolved_auto_fix_severities}}">
        <check if="review_pass lt max_review_passes">
          <action>Set {{review_pass}} = {{review_pass}} + 1</action>
          <action>Set {{review_outcome}} = "fixed"</action>
          <output>🔁 Auto-fix pass complete with remaining fix-required issues. Re-running review pass {{review_pass}} of {{max_review_passes}}.</output>
          <goto step="3" />
        </check>

        <check if="review_pass gte max_review_passes">
          <action>Add "Review Follow-ups (AI)" subsection to Tasks/Subtasks</action>
          <action>For each remaining fix-required issue: `- [ ] [AI-Review][Severity] Description [file:line]`</action>
          <action>Set {{action_count}} = number of action items created</action>
          <action>Set {{review_outcome}} = {{on_max_passes}}</action>
        </check>
      </check>

      <check if="no fix-required issues remain in {{resolved_auto_fix_severities}}">
        <check if="fixed_count gt 0">
          <action>Set {{review_outcome}} = "fixed"</action>
        </check>
        <check if="fixed_count == 0">
          <action>Set {{review_outcome}} = "passed"</action>
        </check>
      </check>
    </check>

    <check if="{{auto_fix_rerun}} != true">
      <ask>What should I do with these issues?

        1. **Fix them automatically** - I'll update the code and tests
        2. **Create action items** - Add to story Tasks/Subtasks for later
        3. **Show me details** - Deep dive into specific issues

        Choose [1], [2], or specify which issue to examine:</ask>

      <check if="user chooses 1">
        <action>Fix issues in severity set {{resolved_auto_fix_severities}}</action>
        <action>Add/update tests as needed</action>
        <action>Update File List in story if files changed</action>
        <action>Update story Dev Agent Record with fixes applied</action>
        <action>Set {{fixed_count}} = number of issues fixed in {{resolved_auto_fix_severities}}</action>
        <action>Set {{action_count}} = 0</action>
        <check if="fix-required issues remain in {{resolved_auto_fix_severities}}">
          <action>Set {{review_outcome}} = "needs_manual"</action>
        </check>
        <check if="no fix-required issues remain in {{resolved_auto_fix_severities}} AND fixed_count gt 0">
          <action>Set {{review_outcome}} = "fixed"</action>
        </check>
        <check if="no fix-required issues remain in {{resolved_auto_fix_severities}} AND fixed_count == 0">
          <action>Set {{review_outcome}} = "passed"</action>
        </check>
      </check>

      <check if="user chooses 2">
        <action>Add "Review Follow-ups (AI)" subsection to Tasks/Subtasks</action>
        <action>For each issue: `- [ ] [AI-Review][Severity] Description [file:line]`</action>
        <action>Set {{action_count}} = number of action items created</action>
        <action>Set {{fixed_count}} = 0</action>
        <action>Set {{review_outcome}} = "needs_manual"</action>
      </check>

      <check if="user chooses 3">
        <action>Show detailed explanation with code examples</action>
        <action>Return to fix decision</action>
        <action>Set {{review_outcome}} = "needs_manual"</action>
      </check>
    </check>
  </step>

  <step n="5" goal="Update story status and sync sprint tracking">
    <!-- Determine new status based on review outcome -->
    <action>Set {{new_status}} = "in-progress"</action>
    <check if="{{review_outcome}} == 'passed'">
      <action>Set {{new_status}} = "done"</action>
      <action>Update story Status field to "done"</action>
    </check>
    <check if="{{review_outcome}} == 'fixed' AND all ACs implemented">
      <action>Set {{new_status}} = "done"</action>
      <action>Update story Status field to "done"</action>
    </check>
    <check if="{{review_outcome}} == 'needs_manual' OR {{review_outcome}} == 'max_passes_exceeded' OR HIGH or MEDIUM issues remain OR ACs not fully implemented">
      <action>Set {{new_status}} = "in-progress"</action>
      <action>Update story Status field to "in-progress"</action>
    </check>
    <action>Save story file</action>

    <!-- Determine sprint tracking status -->
    <check if="{sprint_status} file exists">
      <action>Set {{current_sprint_status}} = "enabled"</action>
    </check>
    <check if="{sprint_status} file does NOT exist">
      <action>Set {{current_sprint_status}} = "no-sprint-tracking"</action>
    </check>

    <!-- Sync sprint-status.yaml when story status changes (only if sprint tracking enabled) -->
    <check if="{{current_sprint_status}} != 'no-sprint-tracking'">
      <action>Load the FULL file: {sprint_status}</action>
      <action>Find development_status key matching {{story_key}}</action>

      <check if="{{new_status}} == 'done'">
        <action>Update development_status[{{story_key}}] = "done"</action>
        <action>Save file, preserving ALL comments and structure</action>
        <output>✅ Sprint status synced: {{story_key}} → done</output>
      </check>

      <check if="{{new_status}} == 'in-progress'">
        <action>Update development_status[{{story_key}}] = "in-progress"</action>
        <action>Save file, preserving ALL comments and structure</action>
        <output>🔄 Sprint status synced: {{story_key}} → in-progress</output>
      </check>

      <check if="story key not found in sprint status">
        <output>⚠️ Story file updated, but sprint-status sync failed: {{story_key}} not found in sprint-status.yaml</output>
      </check>
    </check>

    <check if="{{current_sprint_status}} == 'no-sprint-tracking'">
      <output>ℹ️ Story status updated (no sprint tracking configured)</output>
    </check>

    <!-- ADO MCP Sync: Update work item status based on review outcome -->
    <action>Load "_bmad-output/lens-work/personal/profile.yaml" if it exists; set tracker = profile.preferences.tracker, ado_organization = profile.preferences.ado_organization, ado_project = profile.preferences.ado_project; if profile not found or fields missing, set tracker = "none"</action>
    <action>Check story front matter for "Work Item ID" field and extract {{ado_work_item_id}}</action>
    <check if="tracker == 'azure-devops' AND {{ado_work_item_id}} is not empty">
      <check if="{{new_status}} == 'done'">
        <action>Attempt ADO status sync via MCP (non-blocking):
          mcp_microsoft_azu_wit_update_work_item(
            organization: ado_organization,
            project: ado_project,
            id: {{ado_work_item_id}},
            state: "Closed"
          )
        </action>
        <check if="ADO MCP call succeeds">
          <output>✅ ADO work item #{{ado_work_item_id}} → Closed</output>
          <!-- Optionally add completion comment -->
          <action>Attempt to add comment via MCP (non-blocking):
            mcp_microsoft_azu_wit_add_work_item_comment(
              organization: ado_organization,
              project: ado_project,
              id: {{ado_work_item_id}},
              text: "Story completed via BMAD lens-work code review. Status: done."
            )
          </action>
        </check>
        <check if="ADO MCP call fails or MCP unavailable">
          <output>⚠️ ADO status sync skipped (done). Update ADO work item #{{ado_work_item_id}} manually.</output>
        </check>
      </check>
      <check if="{{new_status}} == 'in-progress'">
        <output>ℹ️ ADO work item #{{ado_work_item_id}} remains Active (review returned to in-progress)</output>
      </check>
    </check>

    <output>**✅ Review Complete!**

      **Story Status:** {{new_status}}
      **Issues Fixed:** {{fixed_count}}
      **Action Items Created:** {{action_count}}

      {{#if new_status == "done"}}Code review complete!{{else}}Address the action items and continue development.{{/if}}
    </output>
  </step>

</workflow>